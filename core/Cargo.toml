[package]
name = "photo_selector_core"
version = "0.1.0"
edition = "2024"

[dependencies]
serde = { version = "1", features = ["derive"], optional = true }

[features]
tauri = ["serde"]

[dev-dependencies]
tempfile = "3"
